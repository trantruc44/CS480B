using NUnit.Framework;
using quotable.api.Controllers;
using quotable.core;

namespace Tests
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void Test_Get_Success()
        {
            //var generator = new SimpleRandomQuoteProvider();
            //var controller = new quoteController(generator);

            //var actual = controller.Get(quotableData);

            //Assert.That(actual.Value.Number, Is.EqualTo(5));
            //Assert.That(actual.Value.Sequence, Is.EquivalentTo(new long[] { 5, 16, 8, 4, 2, 1 }));
        }
    }
}